phases:
    install:
        commands:
            - npm install
    build:
        commands:
            - npm run build
artifacts:
    files:
        - ./dist/*
        - ./dist/**/*
    base-directory: ./trendnineweb/dist
