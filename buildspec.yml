phases:
    install:
        commands:
            - npm install
    build:
        commands:
            - npm run build
artifacts:
    files:
        - './dist/*'
        - './dist/**/*'
    discard-paths: yes
    base-directory: 'trendnineweb/dist'
